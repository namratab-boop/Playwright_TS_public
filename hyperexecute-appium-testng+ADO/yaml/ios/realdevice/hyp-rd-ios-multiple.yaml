version: 0.2
globalTimeout: 150
testSuiteTimeout: 150
testSuiteStep: 150

runson: linux

concurrency: 3

autosplit: true

retryOnFailure: false
maxRetries: 1

runtime:
  - language: java
    version: "8"

pre:
  - mvn dependency:resolve

appium: true
framework:
  name: maven/testng
  defaultReports: false
  discoveryType: xmltest
  flags: ["-Pios-parallel-realdevice"]

jobLabel: ['HYP-RD', 'iOS', 'Multiple Device']

uploadArtefacts:

  - name: FinalReport 
    path:
    - target/**